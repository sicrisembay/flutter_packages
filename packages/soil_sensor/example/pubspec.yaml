name: soil_sensor_example
description: Demonstrates how to use the soil_sensor package.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  soil_sensor:
    path: ../
